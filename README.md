# Guild Manager React

This project is a World of Warcraft-inspired guild management app built with React.

You recruit heroes with different races, classes, roles, and professions, send them on missions, level them up, and improve their gear through loot rewards.

The app lives in `guild-manager/`. Optional Oracle/Gemini features use a server-side proxy; do not put a Gemini API key in the browser build.
